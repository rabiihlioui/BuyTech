import { ClientJoiningDatePipe } from './client-joining-date.pipe';

describe('ClientJoiningDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ClientJoiningDatePipe();
    expect(pipe).toBeTruthy();
  });
});
